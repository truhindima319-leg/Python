# TODO решите задачу
Импорт JSON
def task() -> float:
    с open('input.json', 'r', encoding='utf-8') в качестве файла:
        data = json. load(файл))
    total = sum(product['score'] * item ['weight'] для item in data)
     возвратный раунд (всего, 3)
print(задача)())